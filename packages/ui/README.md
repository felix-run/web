# `@felix/ui`

Shared [shadcn/ui](https://ui.shadcn.com/) primitives for Felix web apps
(Turborepo `packages/ui`).

```ts
import { Button } from '@felix/ui/button';
import { cn } from '@felix/ui/lib/utils';
```

Add components:

```bash
pnpm dlx shadcn@latest add dialog --cwd packages/ui
```
