{
  "name": "@felix/test-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Parameterized behavioral test suites for the contracts this repo has to keep — the proxy Worker and the SSE reader.",
  "exports": {
    "./proxy-worker": "./src/proxy-worker.ts",
    "./sse": "./src/sse.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@felix/typescript-config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
