{
  "name": "@felix/design",
  "version": "0.1.0",
  "private": true,
  "description": "Felix design tokens — the shared neutral palette and theme CSS builders consumed by docs (Starlight) and chat-ui",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "sync:theme": "tsx scripts/sync-theme.ts",
    "lint": "biome check .",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@felix/typescript-config": "workspace:*",
    "tsx": "^4.21.0",
    "typescript": "catalog:"
  }
}
