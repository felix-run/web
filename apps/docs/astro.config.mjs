// Felix docs site — Starlight over the markdown synced from packages/*/docs
// by `scripts/sync-content.ts` (run automatically by the dev/build scripts).
// Static output only; deployed as a Workers static-assets site (wrangler.jsonc).
import starlight from '@astrojs/starlight';
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://docs.felix.run',
  integrations: [
    starlight({
      title: 'Felix',
      description:
        'Felix — a managed agents harness on Cloudflare Workers: manifests, patterns, sessions, governance, and the commerce layer.',
      social: [
        { icon: 'github', label: 'GitHub', href: 'https://github.com/blakebauman/felix-run' },
      ],
      // Shared design system: generated from @felix/harness/design/tokens
      // by scripts/sync-content.ts — the same palette the Scalar API reference
      // uses. Same favicon as the runtime Worker's Scalar page.
      customCss: ['./src/styles/theme.css'],
      head: [
        {
          tag: 'link',
          attrs: { rel: 'icon', href: 'https://make.felix.run/favicon.svg', type: 'image/svg+xml' },
        },
      ],
      sidebar: [
        { label: 'Guide', items: [{ autogenerate: { directory: 'guide' } }] },
        { label: 'Internals', items: [{ autogenerate: { directory: 'internals' } }] },
        { label: 'Commerce', items: [{ autogenerate: { directory: 'commerce' } }] },
        // The API reference is runtime-generated by the Worker itself
        // (Scalar over /openapi.json) — it stays on the deployment.
        { label: 'API reference ↗', link: 'https://make.felix.run/docs' },
      ],
    }),
  ],
});
