// Compatibility shim: re-export the lucide icons the ported chat components
// import under the AI-chatbot's icon-registry names.
import { Paperclip, Square, X } from 'lucide-react';

export const PaperclipIcon = Paperclip;
export const StopIcon = Square;
export const CrossSmallIcon = X;
